<?php 
    // string function
    $username = "Fernando"; 

    // len - contar quantos caracteres a string possui
    $counter = strlen($username);

    // upper - lower
    $upper = strtoupper($username);
    $lower = strtolower($username);

    // trim
    $cortado = trim($username);

?>